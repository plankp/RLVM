	# IT IS NOT RECOMMENDED TO LOAD NEGATIVE NUMBERS DIRECTLY!
	# THEY WILL BE ROUNDED DOWN TO 16 BIT INTEGERS! USE PROPER
	# SUBTRACTION INSTEAD! THE FOLLOWING EXAMPLE IS -2 + (-3)
	.SECTION TEXT
	MOV r0, 0
	NOT r0, r0
	MOV r1, r0
	SUB r0, r0, 1	# mov r0, -2
	SUB r1, r1, 2	# mov r1, -3
	ADD r0, r0, r1	# mov r0, -5
	LDC r1, stdout
	FWRTQ r0, r1, r0
	MOV r0, 0xA
	FWRTB r0, r1, r0
	HALT r5
